# Architecture Decision Records (ADR)

ツールチェーン・lint 方針・型の作り方・UI 基盤など、後から「なぜこうしたのか」を問われる構造的判断を記録する。
何を ADR にするかと、部品をまたぐ作法の説明と手順を `docs/guides/` の設計ガイドへ置くことは ADR-0001 が決める。
**インフラ・ツールチェーン等の構造変更に着手する前に必ず参照する。**

## 一覧

### 文書と ADR

| #                                                                                                                        | タイトル                                                                                                                                          | Status   | Date       | 要約                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [0000](0000-record-one-decision-per-adr-revise-within-its-scope-and-supersede-when-the-decision-changes.md)              | 決定は 1 本に 1 つずつ ADR に記録し、枠内の調整は書き換え、決定が変わったら新しい ADR で置き換える                                                | Accepted | 2026-09-24 | 1 本 1 決定、連番は使い回さない。決定が置き換わったら新しい ADR を起こして旧 ADR を `Superseded` に、同じ決定の枠内の改訂は `Revised` に書く                                                                                                                                               |
| [0001](0001-split-docs-into-layers-limit-adrs-to-significant-choices-and-point-references-one-way.md)                    | ドキュメントは規範・設計ガイド・決定・docstring・履歴の層に分け、ADR は構造・品質特性・戻しにくさに効く選択に限り、参照は規範の側から一方向に張る | Accepted | 2026-09-24 | 規範は `.claude/rules/`、部品をまたぐ作法の説明と手順は `docs/guides/`、構造・主要な品質特性・戻しにくさに効く選択は `docs/decisions/`、1 つの部品に閉じた注意は docstring、変更の記録は git 履歴。ADR とガイドとコードは rules を指さず、コメントは決定なら ADR、作法ならガイドの節を指す |
| [0002](0002-rules-group-one-norm-per-bullet-under-headings-keep-only-what-prevents-mistakes-and-omit-what-lint-stops.md) | rules は見出しと箇条書きで 1 項目 1 規範に分け、消したら誤る規範だけを書き、lint が止めるものは書かない                                           | Accepted | 2026-09-24 | 見出しと箇条書きで 1 項目 1 規範に分け、規範・理由 1 文・出典キー 1 つ (ADR 番号かガイドの節など) を書く。実測と比較は ADR、作法の説明はガイドへ。削除可否は「消したら誤るか」で判定する。lint・型検査・build が止めるものは書かず、lint が案内しない直し方だけを書く                      |
| [0003](0003-place-norms-by-when-they-load-and-bash-only-norms-in-agents-md.md)                                           | 規範の置き場所は読み込まれる契機で決め、Bash で打つだけの操作の規範は AGENTS.md に置く                                                            | Accepted | 2026-09-24 | `paths` の rules は一致するファイルを読んだときにしか読み込まれない。ファイルを読まずに始まる操作の規範は、開始時に読み込まれる AGENTS.md に置く。rules はガイドを import も言葉での誘導もせず、規範を自分で持つ (2026-09-24 の測定)                                                       |

### 開発環境・依存・CI

| #                                                                                    | タイトル                                                                 | Status   | Date       | 要約                                                                                                                                                                                    |
| ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------ | -------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [0008](0008-consolidate-the-dev-toolchain-on-mise-and-vite-plus.md)                  | 開発環境のツールチェーンは mise と Vite+ に寄せる                        | Accepted | 2026-09-20 | Node.js と pnpm の版は `package.json` (`devEngines.runtime` / `packageManager`) が持ち、Vite+ が解決する。mise は tasks と `[env]`。クローン後は `mise install` と `vp install` の 2 段 |
| [0009](0009-wait-three-days-for-dependency-updates-and-give-pins-exit-conditions.md) | 依存更新は待機 3 日で統一し、pin には出口条件を書く                      | Accepted | 2026-09-02 | pnpm の `minimumReleaseAge` と Dependabot の `cooldown` を対で持つ。alerts は `ignore` と独立に発火するので、pin の追従はトリガ駆動で足りる                                             |
| [0010](0010-workflows-are-audited-by-zizmor-and-actions-are-pinned-by-sha.md)        | GitHub Actions の定義は zizmor で検査し、action は commit SHA で固定する | Accepted | 2026-09-23 | `zizmor-action` を `advanced-security: false` で動かし、指摘で job を落とす。`uses:` は `@<SHA> # vX.Y.Z`。`dependabot-cooldown` の下限は ADR-0009 の待機 3 日に合わせる                |

### lint

| #                                                                                          | タイトル                                                                       | Status   | Date       | 要約                                                                                                                             |
| ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------ | -------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------- |
| [0012](0012-select-lint-rules-from-upstream-recommended-and-strict-only-for-typescript.md) | ルールの選定は上流 recommended を基準にし、typescript だけ strict を基準にする | Accepted | 2026-09-20 | カテゴリ有効化は `correctness` と `perf` に限る。off の条件、テストの緩和 5 ルール、jsx-a11y の名指しゼロもここが持つ            |
| [0014](0014-test-only-code-imports-are-blocked-by-no-restricted-imports.md)                | テスト専用コードの import は `no-restricted-imports` で止める                  | Accepted | 2026-09-20 | `*.test-helpers` と `src/test/` をアプリのコードから import させない。範囲は `excludeFiles` で絞り、テストの緩和経路には載せない |

### 配置と境界

| #                                                                                         | タイトル                                                                                     | Status   | Date       | 要約                                                                                                                                                                                      |
| ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | -------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [0015](0015-colocate-domain-code-in-features-and-declare-environments-by-file-suffix.md)  | ドメインに属するコードは `src/features/<domain>/` へ集め、環境はファイル名の接尾辞で宣言する | Accepted | 2026-09-23 | 遮断機構はディレクトリを特別扱いしない (specifier / ファイル名パターン / marker の 3 経路)。ドメインは 1 ディレクトリに集め、`src/server/` はドメインに属さないものを持つ                 |
| [0016](0016-split-components-by-role-and-mark-the-design-system-boundary-by-directory.md) | `src/components/` を役割で分け、design system の著作と消費の境界をディレクトリで表す         | Accepted | 2026-09-19 | `ui/` `action/` `parts/` が著作、`screens/` と直下が消費。`no-restyle` はこの境界を `excludeFiles` で読む。直下は既定で規則が効く側                                                       |
| [0017](0017-server-functions-are-the-data-boundary-and-shared-middleware-is-global.md)    | server function をデータ境界とし、全 fn 共通の middleware は global に載せる                 | Accepted | 2026-09-06 | 認証は `src/start.ts` の `functionMiddleware` へ載せて付け忘れる場所を無くす。`beforeLoad` は画面遷移の UX として残しデータ境界にしない。認可の base builder はロール分けが要るときに足す |
| [0018](0018-derive-domain-types-from-valibot-schemas.md)                                  | ドメイン型は valibot スキーマから導出する                                                    | Accepted | 2026-09-14 | 型は `InferOutput` の型エイリアスで導出し、手書きと二重に持たない。ORM の戻り値は読み出し口で `safeParse` に通す                                                                          |

### 更新と操作

| #                                                                                                                | タイトル                                                                                            | Status   | Date       | 要約                                                                                                                                                                                                                                                                                           |
| ---------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | -------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [0019](0019-leave-memoization-to-react-compiler-and-do-not-force-preventive-memoization.md)                      | メモ化は React Compiler に委ね、予防的なメモ化を強制しない                                          | Accepted | 2026-09-02 | Compiler を `infer` で導入し、適用は `@vitejs/plugin-react` の `compiler` オプション (oxc ネイティブ) で行う。bail out は欠陥として扱わず、`logDiagnostics` でビルドログへ出すだけにする                                                                                                       |
| [0020](0020-user-updates-default-to-transitions-and-query-optimistic-ui-uses-mutation-variables.md)              | ユーザー操作による更新は Transition を既定にし、query 由来の楽観表示は mutation の variables で出す | Accepted | 2026-09-14 | ナビゲーションと GET は同期 Transition、mutation は Action。ダイアログの開閉と入力値は緊急更新のまま。楽観表示は mutation の `variables` を使い、`useOptimistic` は query 外のみ                                                                                                               |
| [0022](0022-mutations-run-from-the-action-prop-via-use-action-mutation-and-double-fire-is-blocked-by-state.md)   | mutation は Action 層の `action` prop から `useActionMutation` で呼び、二重発火は state だけで塞ぐ  | Accepted | 2026-09-14 | `src/components/action/` が `ui/` を包んで `action` prop を受ける。`useActionMutation` は `onError` を型で必須にし `runAction` だけを出す。ref や閉包のフラグは持たない                                                                                                                        |
| [0023](0023-choose-completion-point-and-blocking-scope-per-feature-and-block-only-the-target-item-by-default.md) | ユーザー操作の完了点とブロック範囲は機能ごとに選び、既定は対象の項目だけを止める                    | Accepted | 2026-09-14 | 完了点 (楽観 / サーバー応答 / 再取得完了)、楽観表示の置き場、ブロック範囲の 3 軸で機能ごとに選ぶ。既定は失敗時に戻せるなら楽観、戻せないならサーバー応答で、止めるのは対象の項目だけ。mutation は `onSuccess` で再取得の Promise を返して pending を保ち、対象の項目がそこから busy を表現する |

### 一覧と検索

| #                                                                                                           | タイトル                                                                                              | Status   | Date       | 要約                                                                                                                                                                                                         |
| ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | -------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [0024](0024-build-list-tables-from-tanstack-table-column-definitions-and-render-with-the-registry-table.md) | 一覧テーブルは TanStack Table v9 の列定義で組み、描画は registry の Table に残す                      | Accepted | 2026-09-14 | `createColumnHelper` の列定義を SSOT にし、shadcn Data Table と同じ `DataTable` 部品が registry の `Table` に `FlexRender` で描く。楽観行は行の union で同じ列定義に載せる。楽観表示の設計は ADR-0023 のまま |
| [0025](0025-list-filters-live-in-search-params-and-reach-the-loader-via-loaderdeps.md)                      | 一覧の絞り込み条件は URL の search param が持ち、loaderDeps で loader に渡す                          | Accepted | 2026-09-23 | 条件の schema は server function と route で 1 つ。既定値は `stripSearchParams(v.getDefaults(schema))` で落とし、確定は submit だけで replace。上限は reject せず切り詰め、文字列以外は日本語の文言で弾く    |
| [0026](0026-search-input-is-an-edit-of-the-url-query-and-renders-via-debounce-and-usedeferredvalue.md)      | 検索の入力欄は URL の q に対する編集として持ち、debounce → useDeferredValue → useSuspenseQuery で描く | Accepted | 2026-09-23 | 編集は `q` が変わった世代で紐付け、表示値は描画中に導く (`key={q}` で作り直さない)。編集ごと debounce し、世代が一致するときだけ条件にする。取得中は `StaleContent` で古い一覧を残す                         |

### UI 部品

| #                                                                                                                                 | タイトル                                                                                                           | Status   | Date       | 要約                                                                                                                                                                                                                                       |
| --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | -------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [0027](0027-tell-registry-deviations-apart-by-a-three-way-diff-with-the-generation-baseline-and-limit-them-to-the-allow-lists.md) | registry との乖離は生成時 baseline との 3-way で判別し、許容リスト (registry コードと `src/styles.css`) の行に限る | Accepted | 2026-09-21 | 意図的乖離 = baseline とローカル / 上流 drift = baseline と最新 CLI 出力。registry コードの乖離と行単位の lint 抑制、`src/styles.css` の乖離をそれぞれの許容リストと 1:1 に保つ。抑制はディレクトリ単位の `overrides` ではなく行単位で書く |
| [0028](0028-use-base-ui-number-field-instead-of-type-number.md)                                                                   | 数値入力に `type="number"` を使わず Base UI の NumberField に寄せる                                                | Accepted | 2026-09-02 | `type="number"` は NVDA の要素一覧で unlabeled、Dragon で入力不可、ホイールで値が無言に増減する。推奨形の `type="text"` + `inputmode="numeric"` を出す NumberField を使い、パースとロケール整形も委ねる                                    |
| [0029](0029-field-components-take-a-field-value-prop-for-type-matching.md)                                                        | fieldComponents の部品は値型を突き合わせる `fieldValue` prop を持つ                                                | Accepted | 2026-09-23 | `useFieldContext<T>()` の `T` は実フィールドと結び付かないので、使わない `fieldValue` prop で `field.state.value` の型を受ける。撤去は stable の公開型に値型を突き合わせる API が入ってから                                                |
| [0030](0030-select-value-resolution-does-not-rely-on-base-ui-self-reset.md)                                                       | Select の値の解決は消費側が持ち、Base UI の自己リセットに依存しない                                                | Accepted | 2026-09-23 | 候補から値が消えたときの `onValueChange(null)` は docs に無く、来ない条件があり、版で経路が変わる。`FormSelectField` が値を保持して `console.warn` に残す                                                                                  |

### スタイルとトークン

| #                                                                                                             | タイトル                                                                                    | Status   | Date       | 要約                                                                                                                                                                                                                                                                                                                |
| ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | -------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [0031](0031-keep-consumer-classnames-statically-readable-and-share-appearance-through-components.md)          | 消費側の className は静的に読める形に保ち、共有する外見は部品で配る                         | Accepted | 2026-09-19 | `require-static-classes` を層の境界つきで有効化。`cva` の variant 関数は `variantFunctions` へ宣言する。design system の層から外へ class 文字列を配らない                                                                                                                                                           |
| [0032](0032-colors-are-confined-to-semantic-tokens-by-theme-and-shadcn-lint.md)                               | 色は `@theme` と `@shadcn/lint` の 2 層で semantic token に閉じ込める                       | Accepted | 2026-09-20 | `@theme` の `--color-*: initial` で既定 palette を生成から外し、`@shadcn/lint` を `jsPlugins` で読んで記述時に落とす。ルールは設計判断と対にして 1 つずつ名指しする                                                                                                                                                 |
| [0033](0033-base-semantic-token-values-on-upstream-output-and-decide-deviations-by-wcag-and-palette-steps.md) | セマンティックトークンの値は上流生成物を土台とし、乖離は WCAG の実測と palette の段で決める | Accepted | 2026-09-21 | `src/styles.css` を空ファイルへの生成物から作り直し、`docs/registry-baseline/styles.css` として持つ。動かした値だけを WCAG の実測で正当化し、Tailwind の palette の段に乗せる。用途を兼ねて両立しないトークンは狭い側を切る                                                                                         |
| [0034](0034-placeholders-hold-only-examples-and-use-a-dedicated-color-token.md)                               | placeholder には例示だけを置き、色を専用トークンへ切る                                      | Accepted | 2026-09-21 | 例示は `--placeholder`、ラベルと指示は可視ラベルと `FieldDescription` へ。`select` の空状態は対象外。置いてよいかと色が免除されるかは軸が違い、書式を伝える例示は置けるが不適合になる。axe は `::placeholder` を読まないので緑はこの色の根拠にならない。light は両立する段が 1 つ、dark は無く 4.5:1 を割る側を採る |

### アクセシビリティ

| #                                                                                                      | タイトル                                                                                           | Status   | Date       | 要約                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------- | -------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [0035](0035-announce-status-through-always-mounted-live-regions-and-keep-item-state-in-static-text.md) | 状態の通知は常時 mount の live region に集約し、項目の状態は静的テキストと `aria-busy` で持つ      | Accepted | 2026-09-23 | root に polite / assertive の region を 1 組常時 mount し `announce()` で更新する。項目 (行・ボタン) は `aria-busy` と静的テキストで状態を持ち、live region を条件付きで mount しない。`Spinner` は視覚専用                                                        |
| [0036](0036-query-result-announcements-come-from-the-page-effect-when-the-query-settles.md)            | 取得結果の入れ替わりは、ページの effect が取得の決着で通知し、直前に通知した条件と同じなら出さない | Accepted | 2026-09-23 | ページの `useEffect` が条件と `isFetching` の決着を契機に `announce()` し、`useRef` の直前の条件と同じなら出さない。初期表示は通知しない。ページを `key={q}` で作り直さないことに依存する。ADR-0035 の呼び出し層を query 由来の結果へ広げる                        |
| [0037](0037-contrast-ratios-are-measured-not-transcribed.md)                                           | コントラスト比は測り方を実在させ、動く数値を文書へ書き写さない                                     | Accepted | 2026-09-22 | 測り方を `scripts/contrast/lib/contrast.ts` と `mise run contrast` として置き、トークンを動かすと動く比は文書から落とす。対の一覧は持たない (列挙漏れと、一覧が実際の描画と食い違う乖離を同時に抱え、どちらも検出できない)。検査は作らず、a11y の合否は axe が持つ |
| [0038](0038-axe-incomplete-fails-only-where-rendering-is-controlled.md)                                | axe の incomplete は描画を統制できる層でだけ落とし、`color-contrast` の incomplete は外さない      | Accepted | 2026-09-21 | `incomplete` は「判定できなかった」「JS エラー」「失敗にするのが怖い」が混ざるバケツ。単一部品を描く story では落とし、ブラウザテストでは落とさない。除外は (ルール, messageKey) の粒度                                                                            |
| [0039](0039-do-not-treat-a-green-axe-run-as-proof-and-require-a-measured-count.md)                     | axe の緑を測った証明とせず、測った件数を別に要求する                                               | Accepted | 2026-09-21 | `passes` に入ったことは測った証明にならない (画面外の要素は合格になる)。`resultTypes` と `contrastRatio` の閾値は緑を静かに壊す。axe の担当は 105 ルール / 28 SC で、1.4.11 は 0 ルール                                                                            |
| [0040](0040-a11y-checks-split-roles-by-layer-and-suppressions-sit-in-the-story.md)                     | a11y の検査は層ごとに役割を分け、抑制は出た story に理由と出口を添えて置く                         | Accepted | 2026-09-21 | 静的 lint・devtools・story の axe・ブラウザテストの axe で見る対象と直す場所を分ける。抑制は `parameters.a11y` に置き、理由・本体の扱い・外せる条件を書く                                                                                                          |

### ブラウザテスト

| #                                                                                                           | タイトル                                                                                                                                    | Status   | Date       | 要約                                                                                                                                                                                                                                                                                                                               |
| ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [0041](0041-browser-tests-wait-through-vitest-retry-apis-not-custom-waits.md)                               | ブラウザテストの待機は vitest の retry API に委ね、自前の待機を積まない                                                                     | Accepted | 2026-09-22 | 操作後の mount と状態検証は `expect.element`、消えたことの確認は `expectRemoved()`、`render()` 直後は `element()`、実測は `expect.poll` の中で                                                                                                                                                                                     |
| [0042](0042-browser-test-user-actions-fire-only-real-events.md)                                             | ブラウザテストのユーザー操作は実イベントだけで発火する                                                                                      | Accepted | 2026-09-22 | 合成イベントは使わず helper ごと廃止する。弾かれる要素はキーボードか `.click({ force: true })` で押す。`force` は actionability の検査を飛ばすだけでヒットテストは越えないので、`pointer-events: none` の対象には届かない。二重発火の検証は実イベントを 2 回                                                                       |
| [0043](0043-browser-tests-run-with-animations-off-and-re-enable-them-only-for-animate-out-tests.md)         | ブラウザテストは animation を無効にして走らせ、animate-out の窓を踏むテストだけ戻す                                                         | Accepted | 2026-09-22 | `browser-setup.tsx` が毎テスト `BASE_UI_ANIMATIONS_DISABLED` と `prefers-reduced-motion: reduce` を立て、閉じかけの popup が残る窓と transition の途中値を消す。窓を検証するテストは `enableAnimations()` で戻す。popup を閉じた後の axe は unmount を待つ                                                                         |
| [0044](0044-assertions-take-locators-not-synchronous-reads.md)                                              | assert には locator を渡し、matcher の無い実測は `expect.poll` の中で読む                                                                   | Accepted | 2026-09-22 | 同期読み (`element()` / `query()` / `all()`) を `expect()` へ渡さず `expect.element` を通し、matcher の無い実測は `expect.poll` の中で読む。強制は `jsPlugins` の自前ルール `browser-test/prefer-locator-methods`。適用先はブラウザテストと locator を配る helper で、型が無いぶんを glob が補う。移行は段階を刻まず 1 PR で終える |
| [0045](0045-assert-budget-is-declared-separately-from-the-test-budget.md)                                   | assert の予算をテストの予算と分けて宣言する                                                                                                 | Accepted | 2026-09-22 | `expect.poll.timeout` と `actionTimeout` を対で置き、値は Playwright が文書化した assertion 側の既定 5000ms を写す。`testTimeout` は動かさない。代償として `findElement()` が上限なしになるので呼ばず、`browser-test/no-find-element` が止める                                                                                     |
| [0046](0046-negated-assertions-pass-open-so-write-them-positively.md)                                       | 否定 assert は不在や綴り違いでも通るので、肯定で書く                                                                                        | Accepted | 2026-09-22 | 否定 assert が素通りする経路を 2 つの機構で塞ぐ。期待値がリテラルの否定 (`not.toHaveStyle`、算出値との `not.toBe`) は `browser-test/no-negated-style-literal` が止め、期待値を取らない形 (不在でも通る特例と `toHaveLength`) は `expectAbsent` / `expectRemoved` と肯定 anchor が持つ                                              |
| [0047](0047-route-wrappers-are-tested-by-the-route-file-test-on-a-root-swapped-tree-with-memory-history.md) | Route hooks を使う wrapper は、root を差し替えた route tree に実 Route を付け、memory history の router で route ファイルのテストが検証する | Accepted | 2026-09-23 | 生成済み `routeTree.gen.ts` は `__root.tsx` の devtools と `<html>` で browser test に使えない。実 `Route` を `update({ id, path, getParentRoute })` で test root に付ける。route ファイルのテストは `index.test.tsx`                                                                                                              |

### Storybook

| #                                                                                               | タイトル                                                                                                | Status   | Date       | 要約                                                                                                                                                                                                  |
| ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | -------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [0048](0048-storybook-uses-the-tanstack-framework-and-disables-telemetry.md)                    | Storybook は TanStack 専用の framework で導入し、telemetry を切る                                       | Accepted | 2026-09-20 | `tanstackStart()` plugin と標準の Vite builder が衝突するため。TanStack Query と server-only 依存は自動構成の外で手で置く。テンプレートから配る設定なので `core.disableTelemetry` で telemetry を切る |
| [0049](0049-stories-are-a-state-catalog-and-play-is-written-only-for-interactive-components.md) | story は状態のカタログとし、play は操作で状態が変わる部品にだけ書く                                     | Accepted | 2026-09-20 | story は状態の列挙。`args` で決まる部品には play を書かず、`ui/` の play は開くまで。終了状態が他と同じ見た目の story は `tags: ["!dev"]` で外し、決着しない Promise を置かない                       |
| [0050](0050-play-uses-synthetic-events-and-browser-tests-keep-real-events.md)                   | play は合成イベントで書き、実イベントの規律と移せない検証はブラウザテストに残す                         | Accepted | 2026-09-20 | play へ移した case はブラウザテストから削る。実イベントの規律、レイアウトと配色の実測、型契約はブラウザテストに残し、残す理由を JSDoc に書く                                                          |
| [0051](0051-stories-sit-next-to-components-and-cover-every-registry-component.md)               | story は部品の隣に置き、registry 部品は全件カタログにする                                               | Accepted | 2026-09-20 | 置けるのは `src/components/` 配下。消費側からの import 件数で絞らず、import 0 件の部品にも書く                                                                                                        |
| [0052](0052-tailwind-scans-only-src-and-theme-static-applies-only-to-the-storybook-css.md)      | Tailwind の scan は `src` に絞り、`theme(static)` は Storybook の CSS にだけ掛けて本番の CSS に載せない | Accepted | 2026-09-20 | `@import "tailwindcss" source("../src")` で Markdown の字面を拾わない。全トークンの出力は `.storybook/preview.css` の `theme(static)` だけが持つ                                                      |
| [0053](0053-story-a11y-runs-as-error-with-a-project-per-theme.md)                               | story の a11y は `error` で検査し、テーマごとに project を持つ                                          | Accepted | 2026-09-20 | 違反は抑制せず直す。light と dark の 2 project を持ち、`VITEST_STORYBOOK` が真のときだけ light に絞る。vitest 経由の story の余白は `.storybook/preview.css` が埋める                                 |

## 新しい ADR を書く手順

Status と決定が変わったときの扱いは ADR-0000 が決める。節の立て方と本文の書き方は `docs/guides/writing-docs.md`「ADR を書く」にある。

1. 連番でファイルを作る: `NNNN-<タイトルの英訳を小文字と dash にしたもの>.md`
2. 見出しとメタデータを次の形で書く。タイトル prefix は `ADR-NNNN:`（ハイフン）で統一する

   ```markdown
   # ADR-NNNN: 日本語タイトル

   - Status: Proposed | Accepted | Deprecated | Superseded
   - Date: YYYY-MM-DD
   - （任意）Revised: YYYY-MM-DD (改訂内容の要約)
   - （任意）Supersedes: ADR-NNNN / Superseded-by: ADR-NNNN
   - （任意）関連: ADR-NNNN (関係の一言)
   ```

3. Context / Decision / Consequences の節を書く
4. この README の一覧の、決定が属するテーマの節へ 1 行追記する
5. 既存の決定を置き換えたときは、旧 ADR の本文と一覧の行の Status を `Superseded` にし、`Supersedes` / `Superseded-by` を対で書く

一覧への追記漏れ、一覧のリンク切れ、`Superseded` の書き漏れと `Supersedes` / `Superseded-by` の対の欠けは `scripts/checks/integrity/adr-index.test.ts` が検出する。本文の `ADR-NNNN` の行き先は見ない。
