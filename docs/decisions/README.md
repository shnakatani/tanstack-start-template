# Architecture Decision Records (ADR)

ツールチェーン・lint 方針・型の作り方・UI 基盤など、後から「なぜこうしたのか」を問われる構造的判断を記録する。
**インフラ・ツールチェーン等の構造変更に着手する前に必ず参照する。**

## 一覧

| #                                                                                 | タイトル                                                                                       | Status   | Date                          | 要約                                                                                                                                                                                                                                                                                                                               |
| --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------- | ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [0001](0001-docs-norms-decisions-history-layers.md)                               | ドキュメントを規範と経緯と履歴の 3 層に分ける                                                  | Accepted | 2026-08-17                    | 規範は `.claude/rules/`、経緯と選択肢比較は `docs/decisions/`、変更の記録は git 履歴。rules は 1 項目 200 字以下で、削除可否は「消したら誤るか」で判定する                                                                                                                                                                         |
| [0002](0002-dev-environment-toolchain.md)                                         | 開発環境のツールチェーンは mise と Vite+ に寄せる                                              | Accepted | 2026-08-17（2026-09-02 改訂） | Node.js と pnpm の版は `package.json` (`devEngines.runtime` / `packageManager`) が持ち、Vite+ が解決する。mise は tasks と `[env]`。クローン後は `mise install` と `vp install` の 2 段                                                                                                                                            |
| [0003](0003-plugins-via-explicit-default-set.md)                                  | 有効にするプラグインは既定集合を明示して積む                                                   | Accepted | 2026-08-17                    | `lint.plugins` は既定集合を置換する。明示しないと `typescript` 等が無効になり、`rules` の設定が無診断で捨てられる。既定集合を定数にして spread する                                                                                                                                                                                |
| [0004](0004-lint-rule-selection-criteria.md)                                      | ルールの選定は上流 recommended を基準にし、typescript だけ strict を基準にする                 | Accepted | 2026-08-17（2026-09-13 改訂） | カテゴリ有効化は `correctness` と `perf` に限る。off の条件、テストの緩和 5 ルール、jsx-a11y の名指しゼロ、tailwind 領域を `jsPlugins` で足す判断もここが持つ                                                                                                                                                                      |
| [0005](0005-dependency-supply-chain-gates.md)                                     | 依存更新は待機 3 日で統一し、pin には出口条件を書く                                            | Accepted | 2026-08-17（2026-09-02 改訂） | pnpm の `minimumReleaseAge` と Dependabot の `cooldown` を対で持つ。alerts は `ignore` と独立に発火するので、pin の追従はトリガ駆動で足りる                                                                                                                                                                                        |
| [0006](0006-shadcn-registry-deviation-list.md)                                    | registry コードへの改変は許容リストと生成時 baseline で統制する                                | Accepted | 2026-08-17（2026-09-21 改訂） | 意図的乖離は許容リストと 1:1。判別は `docs/registry-baseline/` を交えた 3-way (意図的乖離 = baseline とローカル / 上流 drift = baseline と最新 CLI 出力)                                                                                                                                                                           |
| [0007](0007-touch-target-aa-baseline.md)                                          | touch target は WCAG 2.2 AA を床とし registry 素寸法で一本化する                               | Accepted | 2026-08-17                    | 44px (HIG / WCAG AAA) を要件にせず 24px を適合の床とする。視覚 = ヒット = registry 素寸法。44px の焼き込みも床もレビューで見る。機械で見るのはポインタが本体へ届かなくなる壊れ方だけ                                                                                                                                               |
| [0008](0008-domain-types-derived-from-schemas.md)                                 | ドメイン型は valibot スキーマから導出する                                                      | Accepted | 2026-08-17（2026-09-14 改訂） | 型は `InferOutput` の型エイリアスで導出し、手書きと二重に持たない。ORM の戻り値は読み出し口で `safeParse` に通す                                                                                                                                                                                                                   |
| [0009](0009-react-compiler-adoption.md)                                           | メモ化は React Compiler に委ね、予防的なメモ化を強制しない                                     | Accepted | 2026-08-17（2026-09-02 改訂） | Compiler を `infer` で導入し、適用は `@vitejs/plugin-react` の `compiler` オプション (oxc ネイティブ) で行う。bail out は欠陥として扱わず、`logDiagnostics` でビルドログへ出すだけにする                                                                                                                                           |
| [0010](0010-number-input-without-type-number.md)                                  | 数値入力に `type="number"` を使わず Base UI の NumberField に寄せる                            | Accepted | 2026-09-02                    | `type="number"` は NVDA の要素一覧で unlabeled、Dragon で入力不可、ホイールで値が無言に増減する。推奨形の `type="text"` + `inputmode="numeric"` を出す NumberField を使い、パースとロケール整形も委ねる                                                                                                                            |
| [0011](0011-server-fn-as-data-boundary.md)                                        | server function をデータ境界とし、全 fn 共通の middleware は global に載せる                   | Accepted | 2026-09-06                    | 認証は `src/start.ts` の `functionMiddleware` へ載せて付け忘れる場所を無くす。`beforeLoad` は画面遷移の UX として残しデータ境界にしない。認可の base builder はロール分けが要るときに足す                                                                                                                                          |
| [0012](0012-colocate-domain-code-by-feature.md)                                   | ドメインに属するコードは `src/features/<domain>/` へ集め、環境はファイル名の接尾辞で宣言する   | Accepted | 2026-09-06（2026-09-14 改訂） | 遮断機構はディレクトリを特別扱いしない (specifier / ファイル名パターン / marker の 3 経路)。ドメインは 1 ディレクトリに集め、`src/server/` はドメインに属さないものを持つ                                                                                                                                                          |
| [0013](0013-browser-test-waiting-via-retry-api.md)                                | ブラウザテストの待機は vitest の retry API に委ね、自前の待機を積まない                        | Accepted | 2026-09-11（2026-09-22 改訂） | 操作後の mount と状態検証は `expect.element`、消えたことの確認は `expectRemoved()`、`render()` 直後は `element()`。`waitForAnimations()` は mount を待たない                                                                                                                                                                       |
| [0014](0014-transitions-by-default-for-user-updates.md)                           | ユーザー操作による更新は Transition を既定にし、pending は Transition から取る                 | Accepted | 2026-09-13（2026-09-14 改訂） | mutation は `src/components/action/` の `action` prop から `useActionMutation` の `runAction` を呼ぶ。ダイアログを閉じる時点とブロック範囲は ADR-0016 の軸で選び、楽観表示は mutation の `variables` を使う (`useOptimistic` は query 外のみ)                                                                                      |
| [0015](0015-browser-test-user-actions-via-real-events.md)                         | ブラウザテストのユーザー操作は実イベントだけで発火する                                         | Accepted | 2026-09-13（2026-09-22 改訂） | 合成イベントは使わず helper ごと廃止する。弾かれる要素はキーボードか `.click({ force: true })` で押す。`force` は actionability の検査を飛ばすだけでヒットテストは越えないので、`pointer-events: none` の対象には届かない。二重発火の検証は実イベントを 2 回                                                                       |
| [0016](0016-completion-point-and-blocking-scope-per-feature.md)                   | ユーザー操作の完了点とブロック範囲は機能ごとに選び、既定は対象の項目だけを止める               | Accepted | 2026-09-14                    | 完了点 (楽観 / サーバー応答 / 再取得完了)、楽観表示の置き場、ブロック範囲の 3 軸で機能ごとに選ぶ。既定は失敗時に戻せるなら楽観、戻せないならサーバー応答で、止めるのは対象の項目だけ。mutation は `onSuccess` で再取得の Promise を返して pending を保ち、対象の項目がそこから busy を表現する                                     |
| [0017](0017-live-region-announcer-for-status.md)                                  | 状態の通知は常時 mount の live region に集約し、項目の状態は静的テキストと `aria-busy` で持つ  | Accepted | 2026-09-14                    | root に polite / assertive の region を 1 組常時 mount し `announce()` で更新する。項目 (行・ボタン) は `aria-busy` と静的テキストで状態を持ち、live region を条件付きで mount しない。`Spinner` は視覚専用                                                                                                                        |
| [0018](0018-browser-tests-disable-base-ui-animations.md)                          | ブラウザテストは Base UI の animation を無効にして走らせ、animate-out の窓を踏むテストだけ戻す | Accepted | 2026-09-14                    | `browser-setup.tsx` が毎テスト `BASE_UI_ANIMATIONS_DISABLED` を立て、閉じかけの popup が残る窓を消す。窓を検証するテストは `enableBaseUiAnimations()` で戻す。popup を閉じた後の axe は unmount を待つ                                                                                                                             |
| [0019](0019-list-tables-via-tanstack-table-column-definitions.md)                 | 一覧テーブルは TanStack Table v9 の列定義で組み、描画は registry の Table に残す               | Accepted | 2026-09-14                    | `createColumnHelper` の列定義を SSOT にし、shadcn Data Table と同じ `DataTable` 部品が registry の `Table` に `FlexRender` で描く。楽観行は行の union で同じ列定義に載せる。楽観表示の設計は ADR-0016 のまま                                                                                                                       |
| [0020](0020-component-layers.md)                                                  | `src/components/` を役割で分け、design system の著作と消費の境界をディレクトリで表す           | Accepted | 2026-09-19                    | `ui/` `action/` `parts/` が著作、`screens/` と直下が消費。`no-restyle` はこの境界を `excludeFiles` で読む。直下は既定で規則が効く側                                                                                                                                                                                                |
| [0021](0021-static-classes-and-shared-appearance.md)                              | 消費側の className は静的に読める形に保ち、共有する外見は部品で配る                            | Accepted | 2026-09-19                    | `require-static-classes` を層の境界つきで有効化。`cva` の variant 関数は `variantFunctions` へ宣言する。design system の層から外へ class 文字列を配らない                                                                                                                                                                          |
| [0022](0022-storybook-as-state-catalog.md)                                        | Storybook を状態のカタログとして使い、操作を伴う検証は play function へ移す                    | Accepted | 2026-09-20                    | story は状態の列挙。操作で状態が変わる部品には play を書き、同じ case を既存テストから削る。トークンは CSS 変数を実測して描く                                                                                                                                                                                                      |
| [0023](0023-storybook-agent-tools-via-cli.md)                                     | Storybook の agent 向けツールは同梱の CLI を使い、MCP addon は入れない                         | Accepted | 2026-09-20                    | `storybook skills` / `storybook tools` を起点にする。MCP の登録は URL を 1 つしか持てず、worktree ごとに変わる port へ配れない                                                                                                                                                                                                     |
| [0024](0024-semantic-token-values-from-upstream-baseline.md)                      | セマンティックトークンの値は上流生成物を土台とし、乖離は WCAG の実測と palette の段で決める    | Accepted | 2026-09-21                    | `src/styles.css` を空ファイルへの生成物から作り直し、`docs/registry-baseline/styles.css` として持つ。動かした値だけを WCAG の実測で正当化し、Tailwind の palette の段に乗せる。用途を兼ねて両立しないトークンは狭い側を切る                                                                                                        |
| [0025](0025-placeholder-examples-only-with-dedicated-token.md)                    | placeholder には例示だけを置き、色を専用トークンへ切る                                         | Accepted | 2026-09-21                    | 例示は `--placeholder`、ラベルと指示は可視ラベルと `FieldDescription` へ。`select` の空状態は対象外。置いてよいかと色が免除されるかは軸が違い、書式を伝える例示は置けるが不適合になる。axe は `::placeholder` を読まないので緑はこの色の根拠にならない。light は両立する段が 1 つ、dark は無く 4.5:1 を割る側を採る                |
| [0026](0026-what-axe-measures-and-what-it-does-not.md)                            | axe で測るのは violations だけとし、incomplete はルールを名指して合否へ入れる                  | Accepted | 2026-09-21                    | `incomplete` は「判定できなかった」「JS エラー」「失敗にするのが怖い」が混ざるバケツ。単一部品を描く story では統制下の判定不能なので落とし、組み上げて操作するブラウザテストでは実行環境の速さで結果が変わるので落とさない。除外は (ルール, messageKey) の粒度。axe の担当は 105 ルール / 28 SC で、1.4.11 は 0 ルール            |
| [0027](0027-a11y-tests-are-split-by-tag-not-project.md)                           | a11y の検査は project ではなく tag で分ける                                                    | Accepted | 2026-09-21                    | 分けたいのは関心と単独実行と runner 設定で、いずれも tag で満たせる。project を足すとそのぶん描画が増える。公式の決定表は横断カテゴリを tags、runner 設定の差を Test Projects に割り当てている。挙動テストの途中の状態を測る assert には tag を付けない (操作の再現が重複する)                                                     |
| [0028](0028-contrast-ratios-are-measured-not-transcribed.md)                      | コントラスト比は測り方を実在させ、動く数値を文書へ書き写さない                                 | Accepted | 2026-09-22                    | 測り方を `scripts/contrast/lib/contrast.ts` と `mise run contrast` として置き、トークンを動かすと動く比は文書から落とす。対の一覧は持たない (列挙漏れと、一覧が実際の描画と食い違う乖離を同時に抱え、どちらも検出できない)。検査は作らず、a11y の合否は axe が持つ                                                                 |
| [0029](0029-assertions-take-locators-not-synchronous-reads.md)                    | assert には locator を渡し、同期読みは代替 matcher の無い実測に限る                            | Accepted | 2026-09-22（2026-09-22 改訂） | 同期読み (`element()` / `query()` / `all()`) を `expect()` へ渡さず `expect.element` を通し、matcher の無い実測は `expect.poll` の中で読む。強制は `jsPlugins` の自前ルール `browser-test/prefer-locator-methods`。適用先はブラウザテストと locator を配る helper で、型が無いぶんを glob が補う。移行は段階を刻まず 1 PR で終える |
| [0030](0030-assert-budget-is-declared-separately-from-the-test-budget.md)         | assert の予算をテストの予算と分けて宣言する                                                    | Accepted | 2026-09-22（2026-09-22 改訂） | `expect.poll.timeout` と `actionTimeout` を対で置き、値は Playwright が文書化した assertion 側の既定 5000ms を写す。`testTimeout` は動かさない。代償として `findElement()` が上限なしになるので呼ばず、`browser-test/no-find-element` が止める                                                                                     |
| [0031](0031-negated-assertions-pass-open-so-write-them-positively.md)             | 否定 assert は不在や綴り違いでも通るので、肯定で書く                                           | Accepted | 2026-09-22                    | 否定 assert が素通りする経路を 2 つの機構で塞ぐ。期待値がリテラルの否定 (`not.toHaveStyle`、算出値との `not.toBe`) は `browser-test/no-negated-style-literal` が止め、期待値を取らない形 (不在でも通る特例と `toHaveLength`) は `expectAbsent` / `expectRemoved` と肯定 anchor が持つ                                              |
| [0032](0032-viewport-containment-is-measured-from-the-rect-not-tobeinviewport.md) | viewport 内に収まることは `toBeInViewport` ではなく矩形の poll で見る                          | Accepted | 2026-09-22                    | `toBeInViewport({ ratio: 1 })` は収まっていても落ちる実行がある (3 回中 2 回、actual ratio 1.000)。`expectWithinViewport` が矩形の 4 辺と寸法を `expect.poll` で読み、はみ出した辺と px を失敗文に残す。部分的に見えることの判定は公式 matcher のまま                                                                              |

## フォーマット規約

新規 ADR は連番でファイルを作る: `NNNN-kebab-case-slug.md`。作成後はこの README の一覧へ 1 行追記する。
追記漏れと参照切れは `scripts/checks/integrity/adr-index.test.ts` が検出する。

### タイトルとメタデータ

```markdown
# ADR-NNNN: 日本語タイトル

- Status: Proposed | Accepted | Deprecated | Superseded
- Date: YYYY-MM-DD
- （任意）Revised: YYYY-MM-DD (改訂内容の要約)
- （任意）Supersedes: ADR-NNNN / Superseded-by: ADR-NNNN
- （任意）関連: ADR-NNNN (関係の一言)
```

- タイトル prefix は `ADR-NNNN:`（ハイフン）で統一する
- 決定が後続 ADR で置き換わったら旧 ADR を `Superseded` にし、相互に `Supersedes` / `Superseded-by` をリンクする
- 同じ ADR の枠内で決定を改訂したときは `Revised` に日付と要約を書き、一覧の Date 欄も `YYYY-MM-DD（YYYY-MM-DD 改訂）` にする。対象は決定の内容が変わる改訂に限る（用語・表現の補足や実測値の追記は `Revised` に載せない）

| Status     | 意味                                            |
| ---------- | ----------------------------------------------- |
| Proposed   | 提案中（レビュー待ち）                          |
| Accepted   | 採用・有効                                      |
| Deprecated | 非推奨（代替なしで使わなくなった）              |
| Superseded | 別 ADR に置き換えられた（`Superseded-by` 併記） |

### セクション構成

最低限 3 つ。題材に応じて調査・設定セクションを足してよい:

- **Context** — 背景・制約・要件。判断の前提を書く
- **Decision** — 何を選んだか。**検討した選択肢の比較表**（候補 / 評価軸 / 採否）と**却下理由**を必ず含める
- **Consequences** — 採用結果として起きること（メリット・デメリット・後続作業・再評価条件）

任意セクション例: 「調査結果」（実測の根拠）、「設定上の注意 / リスク」、「出典」。

### 記述ルール

- 主張は実コード・実測で裏づける。コード参照は `src/path/to/file.ts` のようにファイルパスで書き、半年後も追える形にする
- 決定の文そのものを個別の部品名に依存させない。部品は改名されるが ADR は凍結されるので、決定が宙に浮く。根拠としてのファイルパス参照は上のとおり書いてよい
- 指示語（「本 PR」「今回の」）・チャット内画像参照（`[Image #N]`）を書かない。相対日付は絶対日付（`YYYY-MM-DD`）に変換する（`.claude/rules/doc-hygiene.md`）
- 外部料金・ライブラリ仕様など変動する数値は、前提条件と確認時点を明記し、可能なら出典 URL を「出典」セクションに置く
