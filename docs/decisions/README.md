# Architecture Decision Records (ADR)

ツールチェーン・lint 方針・型の作り方・UI 基盤など、後から「なぜこうしたのか」を問われる構造的判断を記録する。
**インフラ・ツールチェーン等の構造変更に着手する前に必ず参照する。**

## 一覧

| #                                                                                                      | タイトル                                                                                              | Status   | Date       | 要約                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- | -------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [0000](0000-record-one-decision-per-adr-and-supersede-when-it-changes.md)                              | 決定は 1 本に 1 つずつ ADR に記録し、決定が変わったら新しい ADR で置き換える                          | Accepted | 2026-09-23 | 1 本 1 決定、連番は使い回さない。決定が置き換わったら新しい ADR を起こして旧 ADR を `Superseded` に、同じ決定の枠内の改訂は `Revised` に書く                                                                                                                                                                                       |
| [0001](0001-docs-norms-decisions-history-layers.md)                                                    | ドキュメントを規範と経緯と履歴の 3 層に分ける                                                         | Accepted | 2026-08-17 | 規範は `.claude/rules/`、経緯と選択肢比較は `docs/decisions/`、変更の記録は git 履歴。書き方の作法はリポジトリ層の `docs.md` が持つ                                                                                                                                                                                                |
| [0002](0002-dev-environment-toolchain.md)                                                              | 開発環境のツールチェーンは mise と Vite+ に寄せる                                                     | Accepted | 2026-09-20 | Node.js と pnpm の版は `package.json` (`devEngines.runtime` / `packageManager`) が持ち、Vite+ が解決する。mise は tasks と `[env]`。クローン後は `mise install` と `vp install` の 2 段                                                                                                                                            |
| [0003](0003-plugins-via-explicit-default-set.md)                                                       | 有効にするプラグインは既定集合を明示して積む                                                          | Accepted | 2026-08-17 | `lint.plugins` は既定集合を置換する。明示しないと `typescript` 等が無効になり、`rules` の設定が無診断で捨てられる。既定集合を定数にして spread する                                                                                                                                                                                |
| [0004](0004-lint-rule-selection-criteria.md)                                                           | ルールの選定は上流 recommended を基準にし、typescript だけ strict を基準にする                        | Accepted | 2026-09-20 | カテゴリ有効化は `correctness` と `perf` に限る。off の条件、テストの緩和 5 ルール、jsx-a11y の名指しゼロもここが持つ                                                                                                                                                                                                              |
| [0005](0005-dependency-supply-chain-gates.md)                                                          | 依存更新は待機 3 日で統一し、pin には出口条件を書く                                                   | Accepted | 2026-09-02 | pnpm の `minimumReleaseAge` と Dependabot の `cooldown` を対で持つ。alerts は `ignore` と独立に発火するので、pin の追従はトリガ駆動で足りる                                                                                                                                                                                        |
| [0006](0006-shadcn-registry-deviation-list.md)                                                         | registry コードの乖離と行単位の lint 抑制は、この表の行に限る                                         | Accepted | 2026-09-21 | `src/components/ui/` の改変と、その中の lint 抑制の許容リスト。抑制はディレクトリ単位の `overrides` ではなく行単位で書く                                                                                                                                                                                                           |
| [0007](0007-touch-target-aa-baseline.md)                                                               | touch target は WCAG 2.2 AA を床とし registry 素寸法で一本化する                                      | Accepted | 2026-08-17 | 44px (HIG / WCAG AAA) を要件にせず 24px を適合の床とする。視覚 = ヒット = registry 素寸法。44px の焼き込みも床もレビューで見る。機械で見るのはポインタが本体へ届かなくなる壊れ方だけ                                                                                                                                               |
| [0008](0008-domain-types-derived-from-schemas.md)                                                      | ドメイン型は valibot スキーマから導出する                                                             | Accepted | 2026-09-14 | 型は `InferOutput` の型エイリアスで導出し、手書きと二重に持たない。ORM の戻り値は読み出し口で `safeParse` に通す                                                                                                                                                                                                                   |
| [0009](0009-react-compiler-adoption.md)                                                                | メモ化は React Compiler に委ね、予防的なメモ化を強制しない                                            | Accepted | 2026-09-02 | Compiler を `infer` で導入し、適用は `@vitejs/plugin-react` の `compiler` オプション (oxc ネイティブ) で行う。bail out は欠陥として扱わず、`logDiagnostics` でビルドログへ出すだけにする                                                                                                                                           |
| [0010](0010-number-input-without-type-number.md)                                                       | 数値入力に `type="number"` を使わず Base UI の NumberField に寄せる                                   | Accepted | 2026-09-02 | `type="number"` は NVDA の要素一覧で unlabeled、Dragon で入力不可、ホイールで値が無言に増減する。推奨形の `type="text"` + `inputmode="numeric"` を出す NumberField を使い、パースとロケール整形も委ねる                                                                                                                            |
| [0011](0011-server-fn-as-data-boundary.md)                                                             | server function をデータ境界とし、全 fn 共通の middleware は global に載せる                          | Accepted | 2026-09-06 | 認証は `src/start.ts` の `functionMiddleware` へ載せて付け忘れる場所を無くす。`beforeLoad` は画面遷移の UX として残しデータ境界にしない。認可の base builder はロール分けが要るときに足す                                                                                                                                          |
| [0012](0012-colocate-domain-code-by-feature.md)                                                        | ドメインに属するコードは `src/features/<domain>/` へ集め、環境はファイル名の接尾辞で宣言する          | Accepted | 2026-09-23 | 遮断機構はディレクトリを特別扱いしない (specifier / ファイル名パターン / marker の 3 経路)。ドメインは 1 ディレクトリに集め、`src/server/` はドメインに属さないものを持つ                                                                                                                                                          |
| [0013](0013-browser-test-waiting-via-retry-api.md)                                                     | ブラウザテストの待機は vitest の retry API に委ね、自前の待機を積まない                               | Accepted | 2026-09-22 | 操作後の mount と状態検証は `expect.element`、消えたことの確認は `expectRemoved()`、`render()` 直後は `element()`、実測は `expect.poll` の中で                                                                                                                                                                                     |
| [0014](0014-transitions-by-default-for-user-updates.md)                                                | ユーザー操作による更新は Transition を既定にし、pending は Transition から取る                        | Accepted | 2026-09-14 | mutation は `src/components/action/` の `action` prop から `useActionMutation` の `runAction` を呼ぶ。ダイアログを閉じる時点とブロック範囲は ADR-0016 の軸で選び、楽観表示は mutation の `variables` を使う (`useOptimistic` は query 外のみ)                                                                                      |
| [0015](0015-browser-test-user-actions-via-real-events.md)                                              | ブラウザテストのユーザー操作は実イベントだけで発火する                                                | Accepted | 2026-09-22 | 合成イベントは使わず helper ごと廃止する。弾かれる要素はキーボードか `.click({ force: true })` で押す。`force` は actionability の検査を飛ばすだけでヒットテストは越えないので、`pointer-events: none` の対象には届かない。二重発火の検証は実イベントを 2 回                                                                       |
| [0016](0016-completion-point-and-blocking-scope-per-feature.md)                                        | ユーザー操作の完了点とブロック範囲は機能ごとに選び、既定は対象の項目だけを止める                      | Accepted | 2026-09-14 | 完了点 (楽観 / サーバー応答 / 再取得完了)、楽観表示の置き場、ブロック範囲の 3 軸で機能ごとに選ぶ。既定は失敗時に戻せるなら楽観、戻せないならサーバー応答で、止めるのは対象の項目だけ。mutation は `onSuccess` で再取得の Promise を返して pending を保ち、対象の項目がそこから busy を表現する                                     |
| [0017](0017-live-region-announcer-for-status.md)                                                       | 状態の通知は常時 mount の live region に集約し、項目の状態は静的テキストと `aria-busy` で持つ         | Accepted | 2026-09-23 | root に polite / assertive の region を 1 組常時 mount し `announce()` で更新する。項目 (行・ボタン) は `aria-busy` と静的テキストで状態を持ち、live region を条件付きで mount しない。`Spinner` は視覚専用                                                                                                                        |
| [0018](0018-browser-tests-disable-base-ui-animations.md)                                               | ブラウザテストは animation を無効にして走らせ、animate-out の窓を踏むテストだけ戻す                   | Accepted | 2026-09-22 | `browser-setup.tsx` が毎テスト `BASE_UI_ANIMATIONS_DISABLED` と `prefers-reduced-motion: reduce` を立て、閉じかけの popup が残る窓と transition の途中値を消す。窓を検証するテストは `enableAnimations()` で戻す。popup を閉じた後の axe は unmount を待つ                                                                         |
| [0019](0019-list-tables-via-tanstack-table-column-definitions.md)                                      | 一覧テーブルは TanStack Table v9 の列定義で組み、描画は registry の Table に残す                      | Accepted | 2026-09-14 | `createColumnHelper` の列定義を SSOT にし、shadcn Data Table と同じ `DataTable` 部品が registry の `Table` に `FlexRender` で描く。楽観行は行の union で同じ列定義に載せる。楽観表示の設計は ADR-0016 のまま                                                                                                                       |
| [0020](0020-component-layers.md)                                                                       | `src/components/` を役割で分け、design system の著作と消費の境界をディレクトリで表す                  | Accepted | 2026-09-19 | `ui/` `action/` `parts/` が著作、`screens/` と直下が消費。`no-restyle` はこの境界を `excludeFiles` で読む。直下は既定で規則が効く側                                                                                                                                                                                                |
| [0021](0021-static-classes-and-shared-appearance.md)                                                   | 消費側の className は静的に読める形に保ち、共有する外見は部品で配る                                   | Accepted | 2026-09-19 | `require-static-classes` を層の境界つきで有効化。`cva` の variant 関数は `variantFunctions` へ宣言する。design system の層から外へ class 文字列を配らない                                                                                                                                                                          |
| [0022](0022-storybook-as-state-catalog.md)                                                             | story は状態のカタログとし、play は操作で状態が変わる部品にだけ書く                                   | Accepted | 2026-09-20 | story は状態の列挙。`args` で決まる部品には play を書かず、`ui/` の play は開くまで。終了状態が他と同じ見た目の story は `tags: ["!dev"]` で外し、決着しない Promise を置かない                                                                                                                                                    |
| [0023](0023-storybook-agent-tools-via-cli.md)                                                          | Storybook の agent 向けツールは同梱の CLI を使い、MCP addon は入れない                                | Accepted | 2026-09-20 | `storybook skills` / `storybook tools` を起点にする。MCP の登録は URL を 1 つしか持てず、worktree ごとに変わる port へ配れない                                                                                                                                                                                                     |
| [0024](0024-semantic-token-values-from-upstream-baseline.md)                                           | セマンティックトークンの値は上流生成物を土台とし、乖離は WCAG の実測と palette の段で決める           | Accepted | 2026-09-21 | `src/styles.css` を空ファイルへの生成物から作り直し、`docs/registry-baseline/styles.css` として持つ。動かした値だけを WCAG の実測で正当化し、Tailwind の palette の段に乗せる。用途を兼ねて両立しないトークンは狭い側を切る                                                                                                        |
| [0025](0025-placeholder-examples-only-with-dedicated-token.md)                                         | placeholder には例示だけを置き、色を専用トークンへ切る                                                | Accepted | 2026-09-21 | 例示は `--placeholder`、ラベルと指示は可視ラベルと `FieldDescription` へ。`select` の空状態は対象外。置いてよいかと色が免除されるかは軸が違い、書式を伝える例示は置けるが不適合になる。axe は `::placeholder` を読まないので緑はこの色の根拠にならない。light は両立する段が 1 つ、dark は無く 4.5:1 を割る側を採る                |
| [0026](0026-what-axe-measures-and-what-it-does-not.md)                                                 | axe の緑を測った証明とせず、測った件数を別に要求する                                                  | Accepted | 2026-09-21 | `passes` に入ったことは測った証明にならない (画面外の要素は合格になる)。`resultTypes` と `contrastRatio` の閾値は緑を静かに壊す。axe の担当は 105 ルール / 28 SC で、1.4.11 は 0 ルール                                                                                                                                            |
| [0027](0027-a11y-tests-are-split-by-tag-not-project.md)                                                | a11y の検査は project ではなく tag で分ける                                                           | Accepted | 2026-09-21 | 分けたいのは関心と単独実行と runner 設定で、いずれも tag で満たせる。project を足すとそのぶん描画が増える。公式の決定表は横断カテゴリを tags、runner 設定の差を Test Projects に割り当てている。挙動テストの途中の状態を測る assert には tag を付けない (操作の再現が重複する)                                                     |
| [0028](0028-contrast-ratios-are-measured-not-transcribed.md)                                           | コントラスト比は測り方を実在させ、動く数値を文書へ書き写さない                                        | Accepted | 2026-09-22 | 測り方を `scripts/contrast/lib/contrast.ts` と `mise run contrast` として置き、トークンを動かすと動く比は文書から落とす。対の一覧は持たない (列挙漏れと、一覧が実際の描画と食い違う乖離を同時に抱え、どちらも検出できない)。検査は作らず、a11y の合否は axe が持つ                                                                 |
| [0029](0029-assertions-take-locators-not-synchronous-reads.md)                                         | assert には locator を渡し、matcher の無い実測は `expect.poll` の中で読む                             | Accepted | 2026-09-22 | 同期読み (`element()` / `query()` / `all()`) を `expect()` へ渡さず `expect.element` を通し、matcher の無い実測は `expect.poll` の中で読む。強制は `jsPlugins` の自前ルール `browser-test/prefer-locator-methods`。適用先はブラウザテストと locator を配る helper で、型が無いぶんを glob が補う。移行は段階を刻まず 1 PR で終える |
| [0030](0030-assert-budget-is-declared-separately-from-the-test-budget.md)                              | assert の予算をテストの予算と分けて宣言する                                                           | Accepted | 2026-09-22 | `expect.poll.timeout` と `actionTimeout` を対で置き、値は Playwright が文書化した assertion 側の既定 5000ms を写す。`testTimeout` は動かさない。代償として `findElement()` が上限なしになるので呼ばず、`browser-test/no-find-element` が止める                                                                                     |
| [0031](0031-negated-assertions-pass-open-so-write-them-positively.md)                                  | 否定 assert は不在や綴り違いでも通るので、肯定で書く                                                  | Accepted | 2026-09-22 | 否定 assert が素通りする経路を 2 つの機構で塞ぐ。期待値がリテラルの否定 (`not.toHaveStyle`、算出値との `not.toBe`) は `browser-test/no-negated-style-literal` が止め、期待値を取らない形 (不在でも通る特例と `toHaveLength`) は `expectAbsent` / `expectRemoved` と肯定 anchor が持つ                                              |
| [0032](0032-viewport-containment-is-measured-from-the-rect-not-tobeinviewport.md)                      | viewport 内に収まることは `toBeInViewport` ではなく矩形の poll で見る                                 | Accepted | 2026-09-22 | `toBeInViewport({ ratio: 1 })` は収まっていても落ちる実行がある (3 回中 2 回、actual ratio 1.000)。`expectWithinViewport` が矩形の 4 辺と寸法を `expect.poll` で読み、はみ出した辺と px を失敗文に残す。部分的に見えることの判定は公式 matcher のまま                                                                              |
| [0033](0033-list-filters-live-in-search-params-and-reach-the-loader-via-loaderdeps.md)                 | 一覧の絞り込み条件は URL の search param が持ち、loaderDeps で loader に渡す                          | Accepted | 2026-09-23 | 条件の schema は server function と route で 1 つ。既定値は `stripSearchParams(v.getDefaults(schema))` で落とし、確定は submit だけで replace。上限は reject せず切り詰め、文字列以外は日本語の文言で弾く                                                                                                                          |
| [0034](0034-query-result-announcements-come-from-the-page-effect-when-the-query-settles.md)            | 取得結果の入れ替わりは、ページの effect が取得の決着で通知し、直前に通知した条件と同じなら出さない    | Accepted | 2026-09-23 | ページの `useEffect` が条件と `isFetching` の決着を契機に `announce()` し、`useRef` の直前の条件と同じなら出さない。初期表示は通知しない。ページを `key={q}` で作り直さないことに依存する。ADR-0017 の呼び出し層を query 由来の結果へ広げる                                                                                        |
| [0035](0035-search-input-is-an-edit-of-the-url-query-and-renders-via-debounce-and-usedeferredvalue.md) | 検索の入力欄は URL の q に対する編集として持ち、debounce → useDeferredValue → useSuspenseQuery で描く | Accepted | 2026-09-23 | 編集は `q` が変わった世代で紐付け、表示値は描画中に導く (`key={q}` で作り直さない)。編集ごと debounce し、世代が一致するときだけ条件にする。取得中は `StaleContent` で古い一覧を残す                                                                                                                                               |
| [0036](0036-route-wrappers-are-tested-with-a-root-swapped-route-tree-and-memory-history.md)            | Route hooks を使う wrapper は root を差し替えた route tree と memory history で検証する               | Accepted | 2026-09-23 | 生成済み `routeTree.gen.ts` は `__root.tsx` の devtools と `<html>` で browser test に使えない。実 `Route` を `update({ id, path, getParentRoute })` で test root に付け、debounce の待ちは `vi.mock` で広げる。route ファイルのテストは `index.test.tsx`                                                                          |
| [0037](0037-title-search-uses-like-escape-with-an-escaped-bound-pattern.md)                            | title の部分一致は `LIKE ... ESCAPE` で、エスケープ済みのパターンを bind して渡す                     | Accepted | 2026-09-23 | `likeContains(column, text)` が `%` `_` `\` をエスケープして `ESCAPE` と対で bind する (drizzle に helper 無し)。`instr()` は完全一致になるか、`lower()` を挟むと遅いので不採用                                                                                                                                                    |
| [0038](0038-workflows-are-audited-by-zizmor-and-actions-are-pinned-by-sha.md)                          | GitHub Actions の定義は zizmor で検査し、action は commit SHA で固定する                              | Accepted | 2026-09-23 | `zizmor-action` を `advanced-security: false` で動かし、指摘で job を落とす。`uses:` は `@<SHA> # vX.Y.Z`。`dependabot-cooldown` の下限は ADR-0005 の待機 3 日に合わせる                                                                                                                                                           |
| [0039](0039-field-components-take-a-field-value-prop-for-type-matching.md)                             | fieldComponents の部品は値型を突き合わせる `fieldValue` prop を持つ                                   | Accepted | 2026-09-23 | `useFieldContext<T>()` の `T` は実フィールドと結び付かないので、使わない `fieldValue` prop で `field.state.value` の型を受ける。撤去は stable の公開型に値型を突き合わせる API が入ってから                                                                                                                                        |
| [0040](0040-select-value-resolution-does-not-rely-on-base-ui-self-reset.md)                            | Select の値の解決は消費側が持ち、Base UI の自己リセットに依存しない                                   | Accepted | 2026-09-23 | 候補から値が消えたときの `onValueChange(null)` は docs に無く、来ない条件があり、版で経路が変わる。`FormSelectField` が値を保持して `console.warn` に残す                                                                                                                                                                          |
| [0041](0041-tall-dialogs-scroll-inside-via-dialog-scroll-form.md)                                      | viewport 高を超えるダイアログは本体だけを内部スクロールさせ、`DialogScrollForm` で組む                | Accepted | 2026-09-23 | Base UI の Inside scroll の形に送信を持つ中間コンテナを足す。見出しは sticky にせず、backstop で流れる挙動は組み忘れの防御層として許容する                                                                                                                                                                                         |
| [0042](0042-rules-items-keep-only-what-prevents-a-mistake.md)                                          | rules の 1 項目には消したら誤る規範だけを 200 字以下で書き、lint が止めるものは書かない               | Accepted | 2026-09-23 | 規範・理由 1 文・出典キー 1 つを書き、実測と比較は ADR へ。削除可否は「消したら誤るか」で判定する。lint・型検査・build が止めるものは書かず、lint が案内しない直し方だけを書く                                                                                                                                                     |
| [0043](0043-norms-are-placed-by-when-they-are-loaded.md)                                               | 規範の置き場所は読み込まれる契機で決め、Bash で打つだけの操作の規範は AGENTS.md に置く                | Accepted | 2026-09-23 | `paths` の rules は一致するファイルを読んだときにしか読み込まれない。ファイルを読まずに始まる操作の規範は、開始時に読み込まれる AGENTS.md に置く                                                                                                                                                                                   |
| [0044](0044-code-comments-point-to-adrs-not-rules.md)                                                  | ソースと設定のコメントは rules ではなく ADR を指す                                                    | Accepted | 2026-09-23 | 利用者が rules を差し替えても、ADR は番号ごと付いてくる。理由を持つ ADR が無ければコメント自身に理由を書く                                                                                                                                                                                                                         |
| [0045](0045-colors-are-confined-to-semantic-tokens-by-theme-and-shadcn-lint.md)                        | 色は `@theme` と `@shadcn/lint` の 2 層で semantic token に閉じ込める                                 | Accepted | 2026-09-20 | `@theme` の `--color-*: initial` で既定 palette を生成から外し、`@shadcn/lint` を `jsPlugins` で読んで記述時に落とす。ルールは設計判断と対にして 1 つずつ名指しする                                                                                                                                                                |
| [0046](0046-testing-library-rules-apply-only-to-stories-and-story-helpers.md)                          | testing-library のルールは story と story helper にだけ当てる                                         | Accepted | 2026-09-20 | 基準は上流の `flat/react`。`*.test.tsx` は `vitest-browser-react` の同名 API で誤検出するので当てない。`prefer-screen-queries` と `no-node-access` を外し、`no-debugging-utils` を error へ上げる                                                                                                                                  |
| [0047](0047-test-only-code-imports-are-blocked-by-no-restricted-imports.md)                            | テスト専用コードの import は `no-restricted-imports` で止める                                         | Accepted | 2026-09-20 | `*.test-helpers` と `src/test/` をアプリのコードから import させない。範囲は `excludeFiles` で絞り、テストの緩和経路には載せない                                                                                                                                                                                                   |
| [0048](0048-event-handlers-are-sync-and-close-async-work-inside.md)                                    | イベントハンドラは同期関数とし、非同期処理は内側へ閉じる                                              | Accepted | 2026-09-20 | `no-misused-promises` が鳴る形を直す。待たない判断は内側で 1 回だけ `void` か `.catch()` で表明する。JSX での `void` と prop 型の拡張は採らない                                                                                                                                                                                    |
| [0049](0049-registry-changes-are-told-apart-by-a-three-way-diff-with-the-generation-baseline.md)       | registry コードへの改変は生成時 baseline との 3-way で判別し、許容リストと 1:1 に保つ                 | Accepted | 2026-09-21 | 意図的乖離 = baseline とローカル / 上流 drift = baseline と最新 CLI 出力。baseline は `docs/registry-baseline/` に置き、改変の追加は機能上の必要があるときに限る                                                                                                                                                                   |
| [0050](0050-styles-css-deviations-are-limited-to-the-listed-rows.md)                                   | `src/styles.css` の乖離は、この表の行に限る                                                           | Accepted | 2026-09-21 | `docs/registry-baseline/styles.css` との差分と 1:1 の許容リスト。説明コメントはそれが説明する乖離の行が持つ                                                                                                                                                                                                                        |
| [0051](0051-storybook-uses-the-tanstack-framework.md)                                                  | Storybook は TanStack 専用の framework で導入する                                                     | Accepted | 2026-09-20 | `tanstackStart()` plugin と標準の Vite builder が衝突するため。TanStack Query と server-only 依存は自動構成の外で手で置く                                                                                                                                                                                                          |
| [0052](0052-play-uses-synthetic-events-and-browser-tests-keep-real-events.md)                          | play は合成イベントで書き、実イベントの規律と移せない検証はブラウザテストに残す                       | Accepted | 2026-09-20 | play へ移した case はブラウザテストから削る。実イベントの規律、レイアウトと配色の実測、型契約はブラウザテストに残し、残す理由を JSDoc に書く                                                                                                                                                                                       |
| [0053](0053-token-stories-measure-css-variables-and-static-applies-only-to-storybook.md)               | トークンの story は CSS 変数を実測して描き、`static` は Storybook だけに掛ける                        | Accepted | 2026-09-20 | 値は `getComputedStyle` で読み、`useSyncExternalStore` で購読する。`source("../src")` で scan を絞り、`theme(static)` は `.storybook/preview.css` だけが掛ける                                                                                                                                                                     |
| [0054](0054-story-a11y-runs-as-error-with-a-project-per-theme.md)                                      | story の a11y は `error` で検査し、テーマごとに project を持つ                                        | Accepted | 2026-09-20 | 違反は抑制せず直す。light と dark の 2 project を持ち、`VITEST_STORYBOOK` が真のときだけ light に絞る。vitest 経由の story の余白は `.storybook/preview.css` が埋める                                                                                                                                                              |
| [0055](0055-stories-sit-next-to-components-and-cover-every-registry-component.md)                      | story は部品の隣に置き、registry 部品は全件カタログにする                                             | Accepted | 2026-09-20 | 置けるのは `src/components/` 配下。import 0 件の部品にも書く。story は `no-restyle` の適用外なので、渡す `className` は layout に限りレビューで見る                                                                                                                                                                                |
| [0056](0056-axe-incomplete-fails-only-where-rendering-is-controlled.md)                                | axe の incomplete は描画を統制できる層でだけ落とし、`color-contrast` の incomplete は外さない         | Accepted | 2026-09-21 | `incomplete` は「判定できなかった」「JS エラー」「失敗にするのが怖い」が混ざるバケツ。単一部品を描く story では落とし、ブラウザテストでは落とさない。除外は (ルール, messageKey) の粒度                                                                                                                                            |
| [0057](0057-a11y-checks-split-roles-by-layer-and-suppressions-sit-in-the-story.md)                     | a11y の検査は層ごとに役割を分け、抑制は出た story に理由と出口を添えて置く                            | Accepted | 2026-09-21 | 静的 lint・devtools・story の axe・ブラウザテストの axe で見る対象と直す場所を分ける。抑制は `parameters.a11y` に置き、理由・本体の扱い・外せる条件を書く                                                                                                                                                                          |

## 新しい ADR を書く手順

形式・Status・決定が変わったときの扱いは ADR-0000 が決める。

1. 連番でファイルを作る: `NNNN-kebab-case-slug.md`
2. 見出しとメタデータを次の形で書く。タイトル prefix は `ADR-NNNN:`（ハイフン）で統一する

   ```markdown
   # ADR-NNNN: 日本語タイトル

   - Status: Proposed | Accepted | Deprecated | Superseded
   - Date: YYYY-MM-DD
   - （任意）Revised: YYYY-MM-DD (改訂内容の要約)
   - （任意）Supersedes: ADR-NNNN / Superseded-by: ADR-NNNN
   - （任意）関連: ADR-NNNN (関係の一言)
   ```

3. Context / Decision / Consequences の節を書く
4. この README の一覧へ 1 行追記する

追記漏れと参照切れは `scripts/checks/integrity/adr-index.test.ts` が検出する。
