rules:
  dependabot-cooldown:
    config:
      # zizmor 1.30.1 の既定は 7 日 (2026-09-23 確認)。待機は dependabot.yml と pnpm-workspace.yaml の対で 3 日に
      # 揃えており、7 日案は ADR-0009 で却下した。ここは 3 日を下回る設定を拾う下限として持つ
      days: 3
